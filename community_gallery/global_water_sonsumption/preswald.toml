[project]
title = "Global Water Consumption Data Visualization"
version = "0.1.0"
port = 8501
slug = "test-preswald-691238"  # Required: Unique identifier for your project
entrypoint = "hello.py"    # Required: Main script to run when executing preswald run

[branding]
name = "Preswald Project"
logo = "images/logo.png"
favicon = "images/favicon.ico"
primaryColor = "#F89613"

[data.global_water_consumption]
type = "csv"
path = "data/global_water_consumption.csv"

[logging]
level = "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"