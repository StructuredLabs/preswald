[project]
title = "GreenShift"
version = "0.1.0"
port = 8501
slug = "greenshift-660129"  # Required: Unique identifier for your project
entrypoint = "hello.py"    # Required: Main script to run when executing preswald run

[branding]
name = "GreenShift"
logo = "images/logo.png"
favicon = "images/favicon.ico"
primaryColor = "#F89613"

[data.cleaned_energy_co2]
type = "csv"
path = "data/cleaned_energy_co2.csv"


[logging]
level = "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"